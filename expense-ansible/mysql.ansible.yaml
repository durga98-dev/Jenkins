- name: configure mysql server
  hosts: mysql
  become: yes
  tasks:
    - name: Install mysql
      ansible.builtin.dnf:
        name: mysql-server
        state: present

    - name: Start and enable the service
      ansible.builtin.service:
        name: mysqld
        state: started
        enabled: yes

  
